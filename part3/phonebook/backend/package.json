{
  "name": "persons",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rimraf dist && cd ../frontend && npm install && npm run build && xcopy dist ..\\backend\\dist /E /I /Y",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"uibuild\" && git push"
    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
